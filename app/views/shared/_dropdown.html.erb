<%# app/views/shared/_dropdown.html.erb %>
<%#
  Partial generico per un dropdown di DaisyUI.

  Riceve:
  - trigger:         (Obbligatorio) L'HTML per il pulsante che apre il menu.
       Passato come variabile locale.
  - position_classes: (Opzionale) Classi per il posizionamento (es. "dropdown-end", "dropdown-top").
  - width_class:      (Opzionale) Classe per la larghezza del menu (es. "w-52").

  Usa un blocco `yield` per inserire gli elementi <li> del menu.
%>

<% position_classes ||= "dropdown-end" # Default a destra
  width_class ||= "w-52"             # Default a 52 %>

<div class="dropdown <%= position_classes %>">
  <%# 1. Il Trigger: viene renderizzato esattamente come lo passiamo %>
  <label tabindex="0"><%= trigger %></label>

  <%# 2. Il Contenuto: il menu a tendina che contiene gli elementi passati dal blocco %>
  <ul
    tabindex="-1"
    class="dropdown-content menu bg-base-100 rounded-box z-10 p-2 mt-2 shadow-lg border border-base-300 <%= width_class %>"
  >
    <%= yield %>
  </ul>
</div>
